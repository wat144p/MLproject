

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - ALPHAVANTAGE_API_KEY=${ALPHAVANTAGE_API_KEY}
    volumes:
      - ./models:/app/models # Mount models so they persist/update
      - ./experiments:/app/experiments
      - ./data:/app/data
